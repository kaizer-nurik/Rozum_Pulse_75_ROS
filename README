```

colcon build --symlink-install
. ./install/setup.bash
ros2 launch rozum_pulse_75 display.py

```


WSL2 usb connect:
```
usbipd list
usbipd bind --busid 1-1
usbipd attach --wsl --busid 1-1
```

realsense startup
```
usbipd attach --wsl --busid 1-1
```
docker run -it --rm   --net=host   -e DISPLAY=$DISPLAY   -e QT_X11_NO_MITSHM=1   -v $PWD:/workspace/src   -v /tmp/.X11-unix:/tmp/.X11-unix:rw   moveit/moveit2:jazzy-release

apt install rapidjson-dev ros-jazzy-control* ros-jazzy-serial-driver ros-jazzy-asio-cmake-module ros-jazzy-librealsense2

////LEGACY ros2 launch rozum_moveit demo.launch.py

sudo chmod 777 /dev/serial/by-id/usb-FTDI_FT232R_USB_UART_BG004CBE-if00-port0

ros2 launch new_moveit demo.launch.py

. /opt/ros/jazzy/env/bin/activate #(do before colcon build, or you will get import error in python)
ros2 launch realsense_to_scene_transform dual_aruco_with_tf.launch.py
ros2 service call /calibrate_realsense_position std_srvs/srv/Trigger

ros2 launch yolo_bringup yolo.launch.py
cd .. && . ./install/setup.bash

ros2 launch simple_demo demo.launch.py
ros2 service call /pick_place_teddy_to_container std_srvs/srv/Trigger

ros2 launch rosbridge_server rosbridge_websocket_launch.xml

ros2 param set /rozum_joint_controller rozum.speed_percent 50


high_level_manipulator_interface
